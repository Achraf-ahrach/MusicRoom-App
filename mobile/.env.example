# Google OAuth Credentials — copy this file to .env and fill in your values
# Get these from: https://console.cloud.google.com → APIs & Services → Credentials
#
# You need to create 3 OAuth Client IDs in Google Cloud Console:
#
#   1. Android client — needs package name (com.example.musicroom) + SHA-1 fingerprint
#      → This one does NOT go in code. It just needs to exist in the console.
#        Google Play Services auto-matches it by package name + SHA-1.
#
#   2. Web client — created alongside the Android/iOS clients
#      → Goes here as GOOGLE_WEB_CLIENT_ID
#        Used on Android as serverClientId to get an idToken
#
#   3. iOS client — needs bundle ID (com.example.musicroom)
#      → Goes here as GOOGLE_IOS_CLIENT_ID
#        Used on iOS as clientId (since we don't have Firebase GoogleService-Info.plist)
#        Also: paste the REVERSED iOS client ID in ios/Runner/Info.plist → CFBundleURLSchemes

GOOGLE_WEB_CLIENT_ID="469783669296-jmg77fa22j04dnik6mrambtc26qccc14.apps.googleusercontent.com"
GOOGLE_IOS_CLIENT_ID="469783669296-vrfmrpagvj6tlbijel1olsr3kdl9q1t2.apps.googleusercontent.com"
client_secret="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"